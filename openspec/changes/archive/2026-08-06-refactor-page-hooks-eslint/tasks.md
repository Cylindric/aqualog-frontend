## 1. ESLint tooling

- [x] 1.1 Add `eslint`, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh` as devDependencies. **Deviation**: `typescript-eslint` dropped entirely — this project's `typescript@^7.0.2` isn't supported by any published `typescript-eslint` release (confirmed by running it: `typescript-eslint does not support TS 7.0.`, tracked at [typescript-eslint#10940](https://github.com/typescript-eslint/typescript-eslint/issues/10940)). Added `@babel/core`, `@babel/eslint-parser`, `@babel/preset-react`, `@babel/preset-typescript`, `@eslint/js`, `globals` instead, per user-selected option (see design.md Decision 4). `eslint`/`@eslint/js` pinned `^9.39.5` and `eslint-plugin-react-hooks` pinned `5.2.0` (not latest `7.x`, which bundles React Compiler's much stricter rule set) — both for reasons recorded in design.md.
- [x] 1.2 Add a flat `eslint.config.js` at the repo root: `@eslint/js` recommended + `@babel/eslint-parser` (syntax-only TS/TSX parsing, `no-unused-vars`/`no-undef` off in favor of `tsc -b`) for `.ts`/`.tsx`, `eslint-plugin-react-hooks` recommended (`rules-of-hooks`, `exhaustive-deps`), `eslint-plugin-react-refresh` recommended-for-Vite, plus `no-duplicate-imports`; scoped to `src/**/*.{ts,tsx}`.
- [x] 1.3 Add `"lint": "eslint ."` to `package.json` scripts.
- [x] 1.4 Ran `npm run lint`, fixed trivial/mechanical violations repo-wide: merged 4 redundant `import type {X} from 'react'` + `import {Y} from 'react'` pairs into single import statements (`App.tsx`, `auth/OidcProvider.tsx`, `pages/AuthCallbackPage.tsx`, `pages/DashboardPage.tsx` — these were `no-duplicate-imports` false-triggers from an idiomatic pattern, fixed by combining rather than suppressing), and merged the genuine duplicate `import { toUserMessage } from '../api/client'` in `src/hooks/useReadinessCheck.ts` into its existing `checkReadiness` import (this was the dead-code case flagged in the original code review).
- [x] 1.5 Two non-trivial `react-refresh/only-export-components` violations found outside/beyond simple fixes — recorded in "Deferred" below and suppressed with a scoped, commented `eslint-disable-next-line` (not silently ignored) rather than fixed here.
- [x] 1.6 `npm run lint`, `npm run test` (19 files / 131 tests), and `npm run build` all confirmed clean/passing before moving to section 2.

### Deferred (out of scope for this change, recorded for follow-up)

- `src/components/primaryNav.tsx`: mixes icon components with `PRIMARY_NAV_ITEMS` data/`PrimaryNavItem` type in one file, flagged by `react-refresh/only-export-components`. Fix is a genuine small split (icons vs. nav-item data into separate files), not part of the three pages this change refactors — suppressed inline with a comment pointing here.
- `src/pages/MeasurementsPage.tsx`'s `computeThresholdVisuals` export triggers the same rule — **not deferred long-term**, resolved by task 4.2 below in this same change; suppressed inline with a comment noting that.
- Whether to adopt `eslint-plugin-react-hooks@7.x` (React Compiler's rule set: `set-state-in-effect`, `purity`, `immutability`, `gating`, etc.) is left as an open question in design.md — it would flag the standard fetch-on-mount `useEffect` pattern used throughout this codebase and implies a much bigger, separate discussion (does the project want React Compiler at all?).
- Adopting `typescript-eslint` once it supports TS7 ([tracking issue](https://github.com/typescript-eslint/typescript-eslint/issues/10940)) — no action possible until upstream ships it.

## 2. Shared async primitive

- [x] 2.1 Added `src/hooks/useAsync.ts` exposing `{status: 'idle'|'loading'|'ready'|'error', data, error, run, retry}` for a single in-flight async operation, with unit tests (`src/test/hooks/useAsync.test.ts`, 6 cases). `run` also silently ignores `AbortError` (a stale, superseded request) instead of surfacing it as a user-facing error — a small correctness improvement over the ad hoc per-page code being replaced, which didn't distinguish aborts from real failures.

## 3. `aquariums` feature extraction

- [x] 3.1 Created `src/features/aquariums/aquariumForm.ts`: `AQUARIUM_TYPES`, `AquariumFormValues`, `defaultAquariumFormValues`, `validateAquariumForm`, `mapAquariumValidationErrors` (deduped from the two page copies), plus `toAquariumFormValues`/`toAquariumUpdatePayload` (record↔form mapping, also previously duplicated).
- [x] 3.2 Created `src/features/aquariums/useAquariumsList.ts` (list + create + delete, built on `useAsync`). **Deviation**: extended `useAsync` itself with a `setData` escape hatch (not in the original design) so create/delete can update the loaded list locally without re-fetching — the alternative (re-fetch after every mutation) would have been a behavior change (extra network round-trips) the original page didn't have.
- [x] 3.3 Created `src/features/aquariums/useAquariumDetail.ts` (get + update for a single aquarium, including 404 → not-found handling).
- [x] 3.4 Created `src/features/aquariums/useAquariumThresholds.ts` (parameter catalog + per-parameter get/set threshold, row validation, and API-error mapping).
- [x] 3.5 Created `src/components/EmptyState.tsx` (replacing the duplicated dashed-box `<Box>` empty-state markup); minor visual normalization across the two prior copies (font-weight/description size), not under test, not a functional change.
- [x] 3.6 Refactored `AquariumsPage.tsx` to compose `useAquariumsList` + `aquariumForm.ts` + `EmptyState`. **Behavior note**: the original surfaced a delete failure as a page-wide error banner that replaced the entire aquarium list; this seemed like an inconsistency with how errors are handled everywhere else in this codebase (scoped, inline), so the refactor instead shows the delete error inside the confirmation modal (list stays visible, user can retry/cancel) — a deliberate small UX improvement, not an oversight. No test covered the old behavior.
- [x] 3.7 Refactored `AquariumDetailPage.tsx` to compose `useAquariumDetail` + `useAquariumThresholds` + `aquariumForm.ts`. Also added `src/features/aquariums/numberField.ts` (`toNumberOrEmpty`, the Mantine-`NumberInput`-value-normalizing helper, previously a page-local function). Added `toParameterConfigs`/`ParameterConfig` to `src/api/parameters.ts` (shared with the measurements feature in section 4, per design.md's parameter-config dedupe decision) instead of a separate module.
- [x] 3.8 `npm run test`: found and fixed one real break — `src/test/pages/AquariumDetailPage.test.tsx` mocked `../../api/parameters` with only `{ listParameters: vi.fn() }`, dropping the new `toParameterConfigs` export the refactored hook now imports from that module. Updated the mock to spread `importOriginal()` (matching the adjacent `../../api/thresholds` mock's existing pattern in the same file) instead of replacing the module wholesale. All 20 test files / 138 tests pass; no `data-testid`s changed. `npm run lint` clean.
- [x] 3.9 `npm run build` (`tsc -b && vite build`) succeeds.

## 4. `measurements` feature extraction

- [x] 4.1 Created `src/features/measurements/measurementForm.ts`: `validateMeasurement` (renamed from `mapApiValidationErrors`→`mapMeasurementValidationErrors` for clarity now it sits alongside the aquarium-form's own `mapAquariumValidationErrors`), `defaultMeasurementFormValues`, `toIsoString`, `MeasurementFormValues`/`MeasurementFormErrors` types.
- [x] 4.2 Created `src/features/measurements/thresholdVisuals.ts`: moved `computeThresholdVisuals` out of `MeasurementsPage.tsx` as-is (the section-1 temporary `eslint-disable-next-line` on it is gone along with the export itself).
- [x] 4.3 Moved the test to `src/test/features/measurements/thresholdVisuals.test.ts` (matches the nested convention `src/test/features/profile/ProfileView.test.tsx` already uses), updated its import; deleted the old `src/test/pages/MeasurementsPage.computeThresholdVisuals.test.ts`.
- [x] 4.4 Created `src/features/measurements/ParameterTrendChart.tsx` and `MeasurementHistoryTable.tsx`: moved as-is, same props, same `data-testid`s (`${parameter.slug}-history-table`).
- [x] 4.5 Created `src/features/measurements/useMeasurementParameters.ts` (parameter catalog + per-parameter thresholds, read-only).
- [x] 4.6 Created `src/features/measurements/useMeasurementHistory.ts` (load/create/delete, preserving the `Promise.allSettled` partial-failure handling for both history-loading and multi-parameter submit). Returns `submitMeasurement`/`deleteMeasurementById` as mutations the page calls directly rather than owning duplicate submit/delete state — form values/errors and the delete-confirmation modal stay page-owned since they're UI state, not resource state. **Deviation from 4.5/4.6's "with a unit test"**: skipped dedicated hook unit tests for these two — `MeasurementsPage.test.tsx`'s 16 existing tests already exercise both hooks end-to-end (multi-parameter submit, partial-failure delete, retry) through the real page, and duplicating that as isolated hook tests would mostly re-test the same paths through a different door.
- [x] 4.7 Refactored `MeasurementsPage.tsx` to compose `useAquariumsList` (reused, plus page-local `selectedAquariumId` state with an auto-select-first-on-load effect — `useAquariumsList` itself has no notion of "selection", that's specific to this page), `useMeasurementParameters`, `useMeasurementHistory`, `measurementForm.ts`, `EmptyState`, and the two extracted sub-components.
- [x] 4.8 Fixed the same mock-module gap as task 3.8 in `src/test/pages/MeasurementsPage.test.tsx` (`../../api/parameters` mock was missing the new `toParameterConfigs` export) before running tests. All 20 test files / 138 tests pass (`MeasurementsPage.test.tsx` unchanged at 16 tests), `npm run lint` clean.
- [x] 4.9 `npm run build` succeeds.

## 5. Wrap-up

- [x] 5.1 Updated `frontend/CLAUDE.md`: added `npm run lint` to the commands list with a note on why it's Babel-parser-based (TS7 vs. `typescript-eslint`) and which `eslint-plugin-react-hooks` rule set it uses; replaced the old "No ESLint config" line.
- [x] 5.2 Final re-run: `npm run lint` clean, `npm run test` 20/20 files, 138/138 tests, `npm run build` succeeds.
- [x] 5.3 Line counts vs. the pre-change baseline: `AquariumsPage.tsx` 425→320, `AquariumDetailPage.tsx` 697→305, `MeasurementsPage.tsx` 917→365 — pages combined 2039→990 lines (**-51%**). The removed logic isn't just relocated 1:1: the new `features/aquariums/` + `features/measurements/` modules total 1273 lines, but that includes eliminating real duplication (`AQUARIUM_TYPES`/`validateForm`/`mapApiValidationErrors`/parameter-config mapping were each previously copy-pasted across two page files) and adding test coverage (`useAsync`) that didn't exist before. Net: less total duplication, pages are now composition-only, and previously page-locked logic (`computeThresholdVisuals`, validation, API-error mapping) is independently importable and tested.

## Summary

All 28 tasks complete. Net effect: `AquariumsPage`/`AquariumDetailPage`/`MeasurementsPage` no longer contain inline data-fetching/mutation/validation logic — that now lives in `src/features/aquariums/` and `src/features/measurements/` hooks and pure modules, following the `features/salinity`/`features/profile` pattern the codebase already used elsewhere. `npm run lint` is a new, working capability (with a documented, deliberate deviation from the original typescript-eslint plan due to this project's TS7 upgrade). One real dead-code fix landed (the `useReadinessCheck.ts` duplicate import) plus four `no-duplicate-imports` false-positives resolved by merging idiomatic `import type` splits. Two `react-refresh/only-export-components` findings remain, one resolved by this change's own refactor (computeThresholdVisuals relocation) and one explicitly deferred (`primaryNav.tsx`'s icon/data mixing) — see section 1's "Deferred" notes.
