# Required
VITE_API_BASE_URL=http://localhost:8000

# Bearer token for API authentication.
# This is deployment-configured and should not be user-editable at runtime.
# Note: Vite bundles env vars starting with VITE_ into the client JS.
# Only deploy to trusted networks where this exposure is acceptable.
VITE_API_TOKEN=your-bearer-token-here
